#!/usr/bin/env bash

set -e

echo "-----> CHECKING /usr/lib"
ls /usr/lib | grep libxmlsec1 || echo "libxmlsec1 not found in /usr/lib"

echo "-----> CHECKING /usr/local/lib"
ls /usr/local/lib | grep libxmlsec1 || echo "libxmlsec1 not found in /usr/local/lib"